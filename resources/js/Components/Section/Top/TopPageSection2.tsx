import { FC, memo } from "react";
import HistorySection from "./HistorySection";

const TopPageSection2: FC = memo(() => {
    return (
        <section>
            <HistorySection />
        </section>
    )
});

export default TopPageSection2;
